<div class="sections new">
  <h2>Edit New Section</h2>

  <%=	 form_for(:section, :url => {:action => 'update', :id => @section.id}, :class => "section update") do |f| %>

      <%=	 render(:partial => 'form', :locals => {:f => f}) %>
      <div class="form-button">
        <%= submit_tag("Update") %>
      </div>

  <% end %>

</div>